{
    "name": "igloo-router",
    "version": "0.0.1",
    "module": "index.ts",
    "type": "module",
    "files": [
        "build/"
    ],
    "scripts": {
        "dev": "bun --watch examples/basic-routing.ts",
        "test": "bun --watch test lib/index.ts",
        "loadTest": "artillery quick --count 200 --num 10 http://localhost:3000",
        "build:lib": "bun build ./lib/index.ts --outdir ./build --minify"
    },
    "devDependencies": {
        "artillery": "^2.0.0-37",
        "bun-types": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
